package tokyo.isseikuzumaki.vibeterminal.platform

/** External display is not supported on iOS. */
actual fun launchSecondaryDisplay(context: Any?) = Unit

/** External display is not supported on iOS. */
actual fun stopSecondaryDisplay(context: Any?) = Unit
