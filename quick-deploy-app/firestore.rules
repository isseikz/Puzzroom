rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Devices collection - only server-side access
    match /devices/{deviceToken} {
      allow read, write: if false; // Only accessible via Firebase Admin SDK
    }
  }
}
